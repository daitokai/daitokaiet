- flash.each do |name, msg|
  - if msg.is_a?(String)
    .alert.alert-block{class: "alert-#{name == :notice ? 'success' : 'danger'}"}
      %button.close{type: 'button', data: {dismiss: :alert}} ×
      = msg
